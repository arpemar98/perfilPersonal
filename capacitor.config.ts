import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'perfilPersonal',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
