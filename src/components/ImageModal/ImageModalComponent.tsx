import { IonBadge, IonButtons, IonContent, IonHeader, IonIcon, IonItem, IonLabel, IonList, IonListHeader, IonMenu, IonMenuButton, IonMenuToggle, IonModal, IonNote, IonTitle, IonToggle, IonToolbar, } from '@ionic/react';
import { useLocation } from 'react-router-dom';
import { archiveOutline, archiveSharp, book, bookmarkOutline, heartOutline, heartSharp, home, mailOutline, mailSharp, paperPlaneOutline, paperPlaneSharp, people, trashOutline, trashSharp, warningOutline, warningSharp } from 'ionicons/icons';

export default function ImageModalComponent( image:string, label:string ) {
  
  return (
    <IonModal>

    </IonModal>
  );

};